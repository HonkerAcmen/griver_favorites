# Web
fastapi>=0.140.0,<0.141
uvicorn[standard]>=0.51.0,<0.52

# ORM + DB migration（PostgreSQL）
sqlalchemy>=2.0.36,<3
alembic>=1.14.0,<2
psycopg[binary]>=3.2.0,<4

# Settings / env
pydantic-settings>=2.6.0,<3

# 后续 D3 缓存（可选，用时取消注释）
# redis>=5.2.0,<6

# 后续 D4 RabbitMQ（可选，用时取消注释；具体包名随 GoldRiver 宿主对齐）
# aio-pika>=9.5.0,<10

python-dotenv